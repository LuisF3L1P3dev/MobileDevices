// q10

function calcAreaRetangulo(
    largura: number,
    altura: number,
): number{
   const area = largura * altura;
   return area;
}
console.log("Area do retangulo: "+ calcAreaRetangulo(5, 5));